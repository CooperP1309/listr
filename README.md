# listr
A simplified AI auditing service. In the desktop app, just set your platforms API credentials, demand the list of data you need and kick back. The LLM doesn't see any data. It only has access to the API documentation for your service and builds your API query from that.
